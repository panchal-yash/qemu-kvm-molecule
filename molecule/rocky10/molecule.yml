---
driver:
  name: default
  options:
    managed: true

platforms:
  - name: rocky10
    image: ${HOME}/qemu-images/Rocky-10-GenericCloud.latest.x86_64.raw
    memory: 2048
    cpus: 2
    disk_size: 12G
    ssh_port: 2238
    ssh_user: rocky
    ssh_key: ~/.ssh/id_rsa
    disk_interface: virtio

provisioner:
  name: ansible
  log: true
  playbooks:
    create: ../shared/create.yml
    destroy: ../shared/destroy.yml
    converge: ../shared/converge.yml

verifier:
  name: ansible

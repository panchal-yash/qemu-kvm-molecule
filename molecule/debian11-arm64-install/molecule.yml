---
driver:
  name: default
  options:
    managed: true

platforms:
  - name: debian11-arm64-install
    arch: arm64
    image: ${HOME}/qemu-images/debian-11-genericcloud-arm64.raw
    memory: 4096
    cpus: 2
    disk_size: 12G
    ssh_port: 2250
    ssh_user: debian
    ssh_key: ~/.ssh/id_rsa
    disk_interface: virtio

provisioner:
  name: ansible
  log: true
  playbooks:
    create: ../shared/create.yml
    prepare: ../shared/prepare.yml
    destroy: ../shared/destroy.yml
    converge: ../shared/converge.yml

verifier:
  name: ansible

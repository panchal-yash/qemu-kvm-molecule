---
# Reserved hook for per-distro preflight tasks (currently empty).
# All supported distros ship Python >= 3.9 by default, so no setup is needed.
# Keep this file wired into molecule.yml so future preflight work has a home.
- name: Prepare hosts (no-op)
  hosts: all
  gather_facts: false
  tasks: []

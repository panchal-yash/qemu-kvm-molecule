---
# TEMPORARY — points at the bench playbook for host-concurrency measurement.
# To restore production behavior, replace the import below with:
#   - import_playbook: "{{ lookup('env', 'HOME') }}/package-testing/playbooks/pxb_84.yml"
- import_playbook: converge-bench.yml
